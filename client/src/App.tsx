// Stormfall: Last Horizon design contract — the route is intentionally only the full-screen game frame, never a generic site shell.
import GameCanvas from "@/components/GameCanvas";

export default function App() {
  return <GameCanvas />;
}

